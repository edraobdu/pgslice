# Database connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=my_database
DB_USER=postgres
DB_SCHEMA=public

# Cache settings
CACHE_ENABLED=true
CACHE_TTL_HOURS=24
SNIPPY_CACHE_DIR=~/.cache/snippy

# Connection settings
CONNECTION_TTL_MINUTES=30

# Traversal settings
# MAX_DEPTH=10  # Uncomment to limit relationship depth

# Logging
LOG_LEVEL=INFO
