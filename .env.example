# Database Connection (Required)
# For local development: use localhost
# For Docker on Mac/Windows: use host.docker.internal
# For Docker on Linux: use host.docker.internal or --network host
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=postgres
DB_SCHEMA=public

# Database Password
# IMPORTANT: Do NOT store passwords in this file!
# Set PGPASSWORD as an environment variable instead:
#   export PGPASSWORD=your_password
# Or pass it when running make commands:
#   PGPASSWORD=your_password make run-repl

# Cache Settings
CACHE_ENABLED=true
CACHE_TTL_HOURS=24

# Connection Settings
CONNECTION_TTL_MINUTES=30

# Traversal Settings
# MAX_DEPTH=10  # Uncomment to limit relationship traversal depth

# Logging
LOG_LEVEL=INFO

# Output Directory
# Where generated SQL dumps will be saved
# For Docker: this will be mounted from ./dumps/
SNIPPY_OUTPUT_DIR=~/.snippy/dumps
